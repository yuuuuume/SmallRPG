using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ItemInBattleUse : MonoBehaviour
{
    public static ItemInBattleUse instance;
    public Text useItemName, buyItemDescription;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
